using Automation.SharedKernel.Domain.Entities;

namespace Automation.{{ module_name }}.Domain;

public class {{ entity_name }} : Entity<Guid>
{
    public string Name { get; private set; } = default!;
    
    // Add other properties here
    
    private {{ entity_name }}() { } // EF Core
    
    public {{ entity_name }}(string name)
    {
        Id = Guid.NewGuid();
        Name = name;
    }
}
