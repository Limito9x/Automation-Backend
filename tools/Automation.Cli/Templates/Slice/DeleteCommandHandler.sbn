using Automation.{{ module_name }}.Domain;
using Automation.{{ module_name }}.Infrastructure.Persistence;

namespace Automation.{{ module_name }}.Features.{{ plural_entity_name }}.{{ action_name }};

public class {{ action_name }}Handler({{ module_name }}DbContext db)
{
    public async Task<Result> HandleAsync(
        {{ action_name }}Command command,
        CancellationToken ct)
    {
        _ = db;
        // TODO: Implement delete logic here
        
        return Result.Ok();
    }
}
